# افزونه رابط کاربری وب (WebUI Presentation Plugin)

این افزونه یک رابط کاربری کامل، مدرن و واکنش‌گرا (Responsive) برای هسته پیک فراهم می‌کند. این افزونه با استفاده از HTML/CSS/JS خالص (Vanilla JS) نوشته شده و هدف آن سادگی، سرعت بالا و عدم وابستگی به فریم‌ورک‌های سنگین است.

## ✨ ویژگی‌ها

-   **طراحی مدرن**: استفاده از فونت وزیرمتن و طراحی مینیمال.
-   **پشتیبانی از تم**: حالت تاریک (Dark Mode) و روشن.
-   **مدیریت گپ‌ها**: سایدبار برای ایجاد، حذف، تغییر نام و جابجایی بین گپ‌ها.
-   **نمایش غنی پیام‌ها**:
    -   پشتیبانی کامل از **Markdown** (جدول، کد، لیست و ...).
    -   هایلایت سینتکس کدها.
    -   دکمه کپی برای متن پیام‌ها.
    -   نمایش تصاویر داخل حباب پیام.
-   **پشتیبانی از چندرسانه‌ای**:
    -   آپلود و پیش‌نمایش تصویر قبل از ارسال.
    -   **Lightbox** برای مشاهده تصاویر در سایز تمام صفحه.
-   **مدیریت تنظیمات**: رابط کاربری برای تغییر کلیدهای API و مدل‌های سفارشی.

## 📦 نصب و استفاده

این افزونه باید پس از افزونه‌های زیرساختی (Storage و Network) و Providerها ثبت شود.

```javascript
import WebUIPlugin from './plugins/presentation/webUI/index.js';

// در فایل اصلی برنامه (main.js)
await peik.use(new WebUIPlugin('root'));
```

### آرگومان‌های سازنده
کلاس `WebUIPlugin` یک آرگومان می‌گیرد:
-   `rootElementId` (رشته، پیش‌فرض `'root'`): شناسه (ID) المان `div` در فایل HTML که قرار است رابط کاربری در آن رندر شود.

## 📁 ساختار فایل‌ها

```
webUI/
├── components/       # کلاس‌های منطقی اجزای صفحه (Sidebar, MessageList, ...)
├── lib/              # کتابخانه‌های جانبی (مانند markdown-it)
├── styles/           # فایل‌های CSS ماژولار
├── templates/        # قالب‌های HTML و قطعات (Partials)
├── utils/            # ابزارهای کمکی (TemplateLoader, MarkdownService)
├── index.js          # نقطه ورود افزونه
└── uiManager.js      # مدیر مرکزی و هماهنگ‌کننده
```

## 🎨 سفارشی‌سازی

تمام استایل‌های این افزونه از طریق **متغیرهای CSS** قابل تغییر هستند. شما می‌توانید بدون تغییر کد اصلی، رنگ‌ها، فونت‌ها و اندازه‌ها را تغییر دهید.
برای جزئیات بیشتر به [مستندات تم‌دهی](./docs/theming.md) مراجعه کنید.

## 💻 توسعه

اگر قصد دارید قابلیت جدیدی به UI اضافه کنید:
1.  قالب HTML مربوطه را در پوشه `templates` ویرایش یا ایجاد کنید.
2.  استایل‌های CSS را در `styles` اضافه کنید.
3.  یک کلاس کامپوننت در `components` بسازید که منطق آن بخش را مدیریت کند.
4.  کامپوننت جدید را در `uiManager.js` فراخوانی و مدیریت کنید.
